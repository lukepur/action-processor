{
  "name": "action-processor",
  "version": "0.0.1",
  "description": "Apply an ordered list of actions serially to an initial state and return the final state",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive **/*.test.js"
  },
  "engines": {
    "node": ">=10.7.0"
  },
  "author": "luke.purton@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "lodash": "^4.17.10"
  }
}
